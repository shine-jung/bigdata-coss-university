import { SuperAdminView } from 'src/sections/super-admin';

// ----------------------------------------------------------------------

export const metadata = {
  title: '슈퍼 어드민 - 관리자 권한 관리',
};

export default function SuperAdminPage() {
  return <SuperAdminView />;
}

